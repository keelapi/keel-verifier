__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.claude/
.DS_Store
.env
.env.*
!.env.example
*.key
*.p12
*.pfx
build/
dist/
manifest.json
manifest.json.sigstore
# The release-artifact rule above is unanchored and case-insensitive on macOS,
# so it silently swallowed the vendored golden-vector MANIFEST.json. That file
# is test input, not a release artifact, and a fresh clone without it fails
# collection rather than a check.
!tests/fixtures/action_mapping_evidence/MANIFEST.json
manifest.json.tsa.json
sbom.cyclonedx.json
keel_verifier/_release_manifest.json
# Local-only research/workpapers. Do not publish.
_internal-local/
research-local/
compliance-research-local/
